apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: customer
spec:
  hosts:
  - "customer.example.org"
  gateways:
  - customer-gateway
  http:
  - route:
    - destination:
        port:
          number: 8080
        host: customer